1. To get this repository working you first need to download the libtorch libraries from PyTorch website for c++. 
2. Then you have to create build directory and change into it
3. Then type `cmake ..` to start the build process
4. Type `make`
5. Execute with `./a.out`

This will begin process of training the model and asking  you questions regarding the model. 
